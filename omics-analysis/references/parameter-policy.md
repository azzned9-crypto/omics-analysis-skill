# 参数策略

参数分三层管理。

## 第 1 层：组学默认值

每个 assay profile 中都可以给出建议默认值。

这些值只是起点，不是放之四海而皆准的真理。

## 第 2 层：本次运行覆盖值

如果用户明确给了阈值，就使用用户阈值，并标记为用户覆盖。

如果用户没有明确给阈值，就使用组学默认值，并标记为默认采用。

## 第 3 层：审计记录

每次运行都必须记录：

- 过滤规则
- 统计方法
- p 值或校正后 p 值阈值
- fold-change 阈值
- 富集分析背景集
- 上下调或排序向量的构建规则

不能让阈值只存在于代码里，或只存在于图里。

